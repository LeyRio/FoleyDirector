"""External feature extractors used by FoleyDirector.

This sub-package is intentionally empty in the open-source skeleton; each
extractor (CLIP / Synchformer / BigVGAN / audio VAE) should be loaded from
its own upstream repository under ``${DATA_PREFIX}/checkpoints/...`` as
configured in ``configs/train.yaml``.
"""
